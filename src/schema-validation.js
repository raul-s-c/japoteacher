(function(){
  const levels=['N5','N4','N3','N2','N1'], directions=['ja_es','es_ja'];
  function validateExercise(e){const errors=[];if(!e.exercise_id)errors.push('exercise_id vacío');if(!directions.includes(e.direction))errors.push('direction inválida');if(!e.source_text)errors.push('source_text vacío');if(!e.reference_translation)errors.push('reference_translation vacía');if(!levels.includes(e.jlpt_level))errors.push('jlpt_level inválido');if(!Array.isArray(e.topic_tags)||!e.topic_tags.length)errors.push('topic_tags debe incluir al menos una temática');const expected=e.direction==='ja_es'?['ja','es']:['es','ja'];if(e.source_language!==expected[0]||e.target_language!==expected[1])errors.push('idiomas incoherentes con direction');return errors;}
  function validEvaluation(e){const scores=['objective_score','comprehensibility_score','naturalness_score','grammar_score','vocabulary_score','orthography_score','register_score','overall_score'];return e&&scores.every(k=>Number.isFinite(e[k])&&e[k]>=0&&e[k]<=100)&&typeof e.is_acceptable==='boolean'&&typeof e.correct_japanese_sentence==='string'&&e.correct_japanese_sentence.length>0&&Array.isArray(e.kanji_readings)&&e.kanji_readings.every(x=>x&&typeof x.characters==='string'&&typeof x.reading_hiragana==='string'&&typeof x.meaning_es==='string'&&typeof x.explanation_es==='string')&&Array.isArray(e.errors)&&Array.isArray(e.strengths);}
  window.SchemaValidation={validateExercise,validEvaluation};
})();
