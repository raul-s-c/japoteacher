(function(){
  const $=s=>document.querySelector(s),$$=s=>[...document.querySelectorAll(s)];
  function toast(message){const e=$('#toast');e.textContent=message;e.classList.add('show');setTimeout(()=>e.classList.remove('show'),2400)}
  function showView(name){$$('.view').forEach(x=>x.classList.toggle('active',x.id===`view-${name}`));$$('.nav-item').forEach(x=>x.classList.toggle('active',x.dataset.view===name));$('#viewTitle').textContent={hoy:'Hoy',practicar:'Practicar',progreso:'Progreso',ajustes:'Ajustes'}[name];location.hash=name;if(name==='progreso')window.App.renderProgress();if(name==='ajustes')window.App.renderSettings();scrollTo(0,0)}
  function directionName(d){return d==='ja_es'?'Japonés → Español':'Español → Japonés'}
  const esc=value=>String(value??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
  function scoreRows(ev){return [['Significado',ev.objective_score],['Comprensión',ev.comprehensibility_score],['Naturalidad',ev.naturalness_score],['Gramática',ev.grammar_score],['Vocabulario',ev.vocabulary_score],['Ortografía',ev.orthography_score],['Registro',ev.register_score]].map(([n,v])=>`<div class="score-row"><span>${n}</span><div class="mini-bar"><span style="width:${v}%"></span></div><strong>${v}</strong></div>`).join('')}
  function feedback(ev,userAnswer='',options={}){const errors=Array.isArray(ev.errors)?ev.errors:[],strengths=Array.isArray(ev.strengths)?ev.strengths:[],showNext=options.showNext!==false;const comparisons=errors.length?errors.map(error=>`<article class="comparison-item"><div><span>Tu fragmento</span><strong>${esc(error.source_span||userAnswer||'—')}</strong></div><div class="comparison-arrow">→</div><div><span>Propuesta</span><strong>${esc(error.corrected_span||ev.natural_answer||'—')}</strong></div><p>${esc(error.explanation_es||'')}</p></article>`).join(''):`<article class="comparison-item comparison-ok"><p>Tu respuesta conserva correctamente el significado. No se detectaron cambios necesarios.</p></article>`;return `<div class="score-hero"><div class="score-ring" style="--score:${ev.overall_score}%"><strong>${ev.overall_score}%</strong></div><div><h3>${ev.is_acceptable?'¡Buen trabajo!':'Sigue practicando'}</h3><p>${ev.is_acceptable?'Respuesta aceptable':'Conviene revisar la respuesta'}</p></div></div><section class="proposed-translation"><span>Esta es la traducción propuesta</span><strong>${esc(ev.natural_answer||ev.corrected_answer||'—')}</strong><p>${esc(ev.explanation_es||'')}</p></section><section class="answer-comparison"><h4>Comparación con tu respuesta</h4><div class="your-full-answer"><span>Lo que escribiste</span><strong>${esc(userAnswer||'—')}</strong></div>${comparisons}</section><div class="score-list">${scoreRows(ev)}</div>${strengths.length?`<p class="status-good"><strong>Aciertos:</strong> ${strengths.map(esc).join(', ')}</p>`:''}${showNext?'<button class="primary feedback-next" id="nextExerciseButton">Siguiente ejercicio</button>':''}`}
  window.UI={$, $$,toast,showView,directionName,feedback};
})();
